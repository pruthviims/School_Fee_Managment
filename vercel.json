{
  "installCommand": "npm install && npm --prefix admin-console install",
  "buildCommand": "npm --prefix admin-console run build",
  "outputDirectory": "admin-console/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
